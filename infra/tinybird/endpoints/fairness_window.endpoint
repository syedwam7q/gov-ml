DESCRIPTION >
    Public REST endpoint serving the fairness-window pipe.
    Dashboard calls this with a read-only token.

TOKEN read_dashboard READ

NODE endpoint
SQL >
    SELECT * FROM fairness_window
